import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	container: { flex: 1 },
	listContainer: { marginBottom: 24, paddingHorizontal: 24 },
	image: { width: "100%", height: 400, borderRadius: 20, marginTop: 24 },
	backContainer: { position: "absolute", margin: 32 },
	backIcon: { width: 40, height: 40 }
});
